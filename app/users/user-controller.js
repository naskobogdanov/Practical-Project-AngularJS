'use strict';

angular.module('issueTracker.user', ['issueTracker.users.authentication'])

    .config(['$routeProvider', function($routeProvider) {

    }])

    .controller('UserController', ['$scope', 'authentication', function($scope, authentication) {

    }]);